describe('dashboardService', function() {

  beforeEach(module('hassdash'));

  it('should ...', inject(function(dashboardService) {

    //expect(dashboardService.doSomething()).toEqual('something');

  }));

});
