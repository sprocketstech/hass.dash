describe('widgetService', function() {

  beforeEach(module('hassdash'));

  it('should ...', inject(function(widgetService) {

    //expect(widgetService.doSomething()).toEqual('something');

  }));

});
