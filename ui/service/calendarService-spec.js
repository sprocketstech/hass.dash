describe('calendarService', function() {

  beforeEach(module('hassdash'));

  it('should ...', inject(function(calendarService) {

    //expect(calendarService.doSomething()).toEqual('something');

  }));

});
